# Firebase, Google Sign-In and the Flutter plugins used here (firestore,
# storage, auth, google_sign_in, image_picker, flutter_image_compress,
# shared_preferences, sqflite/floor) all ship their own consumer ProGuard
# rules inside their AARs, so nothing project-specific is required for them.
# Empty on purpose — add rules here only if a release build crashes and the
# stack trace points at a stripped/obfuscated class.
